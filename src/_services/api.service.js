import { JwtService } from './jwt.service';

class ApiService extends JwtService {
  constructor (config) {
    super(config);
  }

  // request
  // request with auth

  // other api calls fns

}

export { ApiService };
