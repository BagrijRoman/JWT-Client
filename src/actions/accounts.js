export const TEST_ACTION = 'TEST::ACTION';

const signIn = ({ username, password }) => async (dispathc, getState) => {
  // await request helper

};


export const testAction = () => ({
  type: TEST_ACTION,
  payload: null,
});